# Surebet Scanner — Fonbet × Olimp

Сканер живых вилок (surebets) по **тоталам и форам** между **Fonbet** и **Olimp**
с расчётом сумм ставок. Нашёл вилку → жмёшь **«Перейти»** → открывается именно
этот матч у букмекера, дальше ставишь сам.

## Как работает

```
парсеры (Fonbet, Olimp) → события {тоталы[], форы[]}
   → матчинг команд (транслит + токены + зеркальные пары)
   → поиск вилок по совпадающим линиям → расчёт сумм
   → дашборд: вилка + кнопки «Перейти» на каждый матч
```

Вилка по 2 исходам: кэф `k` ⇒ вероятность `1/k`. Surebet, когда
`1/k1 + 1/k2 < 1`; прибыль `(1/(1/k1+1/k2) − 1)·100%`; ставки делятся так, чтобы
оба исхода давали одинаковый возврат. Для тоталов/фор сравниваются только
**совпадающие линии** (ТБ 2.5 ↔ ТМ 2.5; Ф1 −1.5 ↔ Ф2 +1.5).

## Структура

```
server.js                — Express, цикл сканирования, поиск вилок, API
lib/match.js             — матчинг команд между БК (транслит, токены, reverse)
lib/arb.js               — математика вилок (индекс, прибыль, сплит ставок)
scrapers/parserFonbet.js — Fonbet через JSON-API (без браузера)
scrapers/parserOlimp.js  — Olimp через JSON-API (без браузера)
public/index.html        — дашборд: сырые данные, вилки, суммы, «Перейти»
test/                    — тесты (node --test)
```

## Запуск

```bash
npm install
npm start            # http://localhost:3000
npm test             # тесты
```

### Переменные окружения

| Переменная | Назначение | По умолч. |
|---|---|---|
| `PORT` | порт сервера | 3000 |
| `SCAN_MS` | период сканирования | 5000 |
| `DEFAULT_STAKE` | банк по умолчанию | 10000 |
| `MIN_DISPLAY_PROFIT` | показывать вилки от X% | 0 |
| `FONBET_LINE_URL` | домен Fonbet CDN | line52w.bk6bba-resources.com |
| `FONBET_SITE_URL` | домен Fonbet | https://fonbet.kz |
| `OLIMP_BASE_URL` | домен Olimp | https://olimpbet.kz |

## Дисклеймер

Скрейпинг сайтов букмекеров нарушает их ToS. Только для образовательных целей, на свой риск.
